import {FormControl} from "@angular/forms";



export function isMajusclePrefix(control:FormControl) {

  //TODO code metier pour verifier que le prefix de l'input soit bien une majuscule
return null;
}
